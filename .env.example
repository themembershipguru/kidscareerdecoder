# Copy this file to `.env` and fill in real values (`.env` is gitignored).
# Do not put secrets in `.env.example` if you commit it.

# Supabase → Project Settings → API
SUPABASE_URL=https://anlnwwcinahwoeavrrjz.supabase.co
# Server only — never expose the service role key in the browser or in Vite
SUPABASE_SERVICE_ROLE_KEY=6z26buHGdfXOOm9c

PORT=3001

# Optional: full API origin for `npm run build` / preview without proxy (e.g. http://localhost:3001).
# Leave unset during `npm run dev` to use Vite proxy → same-origin /api.
# VITE_API_URL=http://localhost:3001
